# Assignments
a = 1
b = 2
c = 3
x = a + b

# List
nums = [1, 2, 3, 4]
empty_list = []

# Tuple
coords = (10,20)
nested_tuple = ((1, 2), (3, 4))

# Diccionaries
person = {"name": "Alice", "age": 30}

empty_dict = {}

data = {"sum": 1 + 2, "nested": {"ok": True, "list": [1, 2, 3]}}

mixed = [1, (2, 3), {"a": 4, "b": [5, 6]}]

# If / Elif / Else
if x > 5:
    result = x * 2
    pass
elif x == 3:
    result = x / 2
else:
    result = 0

# While
counter = 0
while counter < 3:
    counter += 1
    if counter == 2:
        continue
    if counter == 3:
        break

# For loop
for i in range(2):
    print(i)

nested_list = [1, [2, [3, [4, [5]]]]]

nested_tuple = (1, (2, (3, (4, 5))))

nested_dict = {
    "level1": {
        "level2": {
            "level3": {
                "numbers": [1, 2, 3],
                "letters": ("a", "b", "c"),
                "flag": True
            }
        }
    }
}

if a < 5:
    for i in range(2):
        while b < 4:
            c = c + 1
            if c == 5:
                nested_list.append(c)
            else:
                nested_dict["level1"]["level2"]["level3"]["extra"] = c
            b += 1
else:
    nested_list = []

final_structure = [
    nested_list,
    nested_tuple,
    nested_dict,
    [{"inner_list": [0, 1, {"deep": [10, 20]}]}]
]
